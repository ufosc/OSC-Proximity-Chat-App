export interface LocationContextProps {
    longitude: number;
    latitude: number;
    isLocationEnabled: boolean;
  }
  
export interface LocationType {
    longitude: number;
    latitude: number;
  }